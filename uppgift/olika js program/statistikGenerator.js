function statistikGenerator(array, operation) {
    let array = [10, 20, 30, 40];
}
switch (operation) {
    case "sum":
        for (let index = 0; index < array.length; index++) {
            summa += array[index];
            
        }
        break;

    default:
        break;
}

console.log(summa);
